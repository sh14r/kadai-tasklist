<%= form_with(model: task, local: true) do |f| %>
<% if task.errors.any? %>
    <div id="error_explanation">
      <ul>
        <% task.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <%= f.label :status, 'ステータス' %>
  <%= f.text_field :status, placeholder: "例)未完了" %>

  <%= f.label :content, 'タスク' %>
  <%= f.text_field :content, placeholder: "例)〇〇プロジェクト" %>

  <%= f.submit '投稿' %>
<% end %>